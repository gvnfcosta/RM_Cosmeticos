// import 'package:provider/provider.dart';

// import 'package:rm/src/models/user_list.dart';
// import 'package:rm/src/models/user_model.dart';

// bool Admin(context) {
//   bool isAdmin = false;
//   final List<UserModel> users = Provider.of<UserList>(context).usuario.toList();

//   if (users.isNotEmpty) isAdmin = users.first.level == 0;
//   return isAdmin;
// }
